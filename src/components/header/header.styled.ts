import styled from "@emotion/styled";

export const StyledHeader = styled.header`
    display: flex;
    align-items: center; /* Выравнивание по вертикали */
    background:linear-gradient(180deg, #fbd07e 0%, #fffefd 100%);
    font-family: var(--main-font);
`
